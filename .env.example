# Public base URL of your prediction API.
# NOTE: VITE_* vars are embedded into the built JS and are visible to anyone.
# This is fine for a public API URL. Never put secrets (keys, tokens) here.
VITE_API_URL=https://web-production-67e26.up.railway.app
